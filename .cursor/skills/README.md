# Editable engineering skills

Copies of [Matt Pocock's skills](https://github.com/mattpocock/skills) (MIT), pinned to commit `8b78b531ab965735c5dc74f6f7a219e1e37326df`.

These are ordinary files this repository owns. They are **not** the Claude Code marketplace plugin (`mattpocock-skills`). Do not install that plugin alongside these copies.

Installed skills:

- `setup-matt-pocock-skills`
- `tdd`
- `codebase-design`
- `diagnosing-bugs`
- `improve-codebase-architecture`
- `grill-with-docs`
- `implement`
- `code-review`
- `domain-modeling`

The same tree is mirrored under `.agents/skills/` for provider-neutral agents. Per-repo setup lives in `docs/agents/`.
