Create a file named `result.txt` in the current working directory.

Its complete content must be exactly this one lowercase word followed by a newline:

orchestrated

Do not change `TASK.md`, `verify.sh`, or any other file.
